services:
  database:
    image: postgis/postgis:18-3.6-alpine
    ports:
      - 5432:5432
    volumes:
      - postgres-data:/var/lib/postgresql
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: limopti

volumes:
  postgres-data:
